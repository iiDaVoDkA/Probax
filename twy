
  
Read-only investigation. Do not modify code.

Find the database model for Initiative tasks and how their
assignees are stored. Focus on Initiative, not Project tasks.

Return:
1. The model file path and class name.
2. The exact fields or relationships linking a task to:
   - an assigned user;
   - a team;
   - an assessment.
3. Any separate assignment model used by those relationships.

Include only the relevant code snippets.
If something is not found, say so. Do not guess or propose changes.




    updateInitiative: (initiativeId: string, values: any) =>
      dispatch(updateInitiativePipeline(initiativeId, values)),
    getInitiativePipeline: () =>
      dispatch(getInitiativePipeline()),
  }),
)(CommitteeDateCellComponent);

So the behavior becomes:

ADMIN
→ sees ✎
→ can edit
Initiative Owner of THIS row
→ sees ✎
→ can edit
Anyone else
→ sees only the date
→ no ✎
→ cannot enter edit mode

One thing you must verify

This line:

original?.initiativeOwnerId === user?.id

is the only field name I don’t want you to blindly copy.

Your table currently has:

accessor: 'initiativeOwner'

but that looks like the display value. Inspect:

console.log(original);

and find the actual owner identifier. It may be something like:

original.initiativeOwner.id

instead of:

original.initiativeOwnerId

If it is nested, then simply use:

const isInitiativeOwner =
  original?.initiativeOwner?.id === user?.id;

Do the same canEdit logic in the Committee ID cell afterward.

And even with this frontend check, keep the backend authorization too; hiding the pen is UX, while the API must remain the actual security check.