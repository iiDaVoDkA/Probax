const CommitteeDateCellComponent = ({ value }: any) => (
  <div>
    {value || '-'} ✎
  </div>
);

const CommitteeDateCell = CommitteeDateCellComponen



Cell: (row: any) => (
  <span>
    {row.value || '-'}

    <button
      type="button"
      onClick={(event) => {
        event.stopPropagation();
        console.log('EDIT COMMITTEE DATE');
      }}
    >
      ✎
    </button>
  </span>
),